function getFullURL(){
	var newURL = window.location.protocol + "//" + window.location.host;
	return newURL;
}
function getURLProtocall(){
	var newURL = window.location.protocol + "//";
	return newURL;
}